{
  "name": "my-node-app",
  "version": "1.0.0",
  "scripts": {
    "_comment1": "테스트 실행: Jest가 tests/ 폴더의 *.test.js 파일을 자동 탐색",
    "test": "jest",
    "_comment2": "빌드 실행: esbuild로 번들링 + 압축 + Node.js 플랫폼 타겟 지정",
    "build": "esbuild src/index.js --bundle --minify --platform=node --outfile=dist/index.js"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "esbuild": "^0.19.0"
  }
}